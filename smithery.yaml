name: usgs-water-mcp
description: MCP server for USGS water data
runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    required: []